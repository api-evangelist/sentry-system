naftiko: 1.0.0-alpha2
info:
  label: Sentry Integration Platform API — Installations
  description: 'Sentry Integration Platform API — Installations. 1 operations. Lead operation: Sentry List an organization''s
    Integration Platform installations. Self-contained Naftiko capability covering one Sentry System business surface.'
  tags:
  - Sentry System
  - Installations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SENTRY_SYSTEM_API_KEY: SENTRY_SYSTEM_API_KEY
capability:
  consumes:
  - type: http
    namespace: sentry-integration-platform-installations
    baseUri: https://sentry.io/api/0
    description: Sentry Integration Platform API — Installations business capability. Self-contained, no shared references.
    resources:
    - name: organizations-organization_id_or_slug-sentry-app-installations
      path: /organizations/{organization_id_or_slug}/sentry-app-installations/
      operations:
      - name: listorganizationinstallations
        method: GET
        description: Sentry List an organization's Integration Platform installations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.SENTRY_SYSTEM_API_KEY}}'
  exposes:
  - type: rest
    namespace: sentry-integration-platform-installations-rest
    port: 8080
    description: REST adapter for Sentry Integration Platform API — Installations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/organizations/{organization-id-or-slug}/sentry-app-installations
      name: organizations-organization-id-or-slug-sentry-app-installations
      description: REST surface for organizations-organization_id_or_slug-sentry-app-installations.
      operations:
      - method: GET
        name: listorganizationinstallations
        description: Sentry List an organization's Integration Platform installations
        call: sentry-integration-platform-installations.listorganizationinstallations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: sentry-integration-platform-installations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Sentry Integration Platform API — Installations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: sentry-list-organization-s-integration-platform
      description: Sentry List an organization's Integration Platform installations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: sentry-integration-platform-installations.listorganizationinstallations
      outputParameters:
      - type: object
        mapping: $.
